numpy>=1.24
trimesh>=4.0
scipy>=1.11
matplotlib>=3.7
laspy>=2.5          # reads ALS LiDAR (.las) data from archive.zip

# Optional — needed for grow_video.py to write MP4 directly.
# If not installed, the script falls back to ffmpeg subprocess.
imageio>=2.28
imageio-ffmpeg>=0.4
